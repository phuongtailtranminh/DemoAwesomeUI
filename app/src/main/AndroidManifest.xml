<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.phuon.demoawesomeui">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="icon, label">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimationActivity" />
        <activity android:name=".SweetAlertActivity" />
        <activity android:name=".ShimmerActivity" />
        <activity android:name=".SwipeMenuActivity" />
        <activity android:name=".SliderActivity" />
        <activity android:name=".NumberProgressActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".SearchViewActivity" />
        <activity android:name=".FloatingActionButtonActivity" />
        <activity android:name=".PullRefreshLayoutActivity" />
        <activity android:name=".FlatButtonActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".BarChartActivity" />
        <activity android:name=".StackedBarchartActivity" />
        <activity android:name=".PieChartActivity" />
        <activity android:name=".LineChartActivity" />
        <activity android:name=".CircleImageViewActivity" />
        <activity android:name=".KenBurnsViewActivity" />
        <activity android:name=".BookLoadingActivity"></activity>
    </application>

</manifest>
